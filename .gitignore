# data (MIT but bulky; reproduce via scripts/01)
data/raw/*
data/processed/*
!data/**/.gitkeep

# results: keep cases + metrics (small, the actual deliverable the README and
# UI depend on). Ignore predictions/ (raw LLM outputs, regenerable, larger).
results/predictions/*
!results/predictions/.gitkeep

# resumable-run scratch state and run logs -- regenerable, not a result artifact
results/_checkpoints/
logs/

# secrets
.env

# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# notebooks
.ipynb_checkpoints/

# web (Next.js)
web/node_modules/
web/.next/
web/next-env.d.ts

# editor / OS
.DS_Store

# coding-agent scaffolding (dev-environment config, not part of the research)
.claude/
